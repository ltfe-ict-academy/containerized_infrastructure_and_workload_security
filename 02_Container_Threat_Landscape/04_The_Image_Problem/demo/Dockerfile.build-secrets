# syntax=docker/dockerfile:1.7
FROM alpine:3.21

RUN --mount=type=secret,id=demo_token \
    TOKEN="$(cat /run/secrets/demo_token)" \
    && [ -n "$TOKEN" ] \
    && echo "Build secret consumed without baking it into the image"
